@prefix sf: <https://specforge.dev/vocab/> .
@prefix dcterms: <http://purl.org/dc/terms/> .
@prefix prov: <http://www.w3.org/ns/prov#> .
@prefix requirement: <https://specforge.dev/requirement/> .
@prefix verification: <https://specforge.dev/verification/> .
@prefix assertion: <https://specforge.dev/assertion/> .
@prefix source: <https://specforge.dev/source/> .

# DATA-001 fordert ein fachlich gültiges Event-Zeitintervall.
requirement:DATA-001-1.0.0 a sf:RequirementDefinition ;
  # Diese Aussage legt die stabile Kennung der Ressource fest.
  dcterms:identifier "DATA-001" ;
  # Diese Aussage pinnt die konkrete Version der Ressource.
  dcterms:hasVersion "1.0.0" ;
  # Diese Aussage beschreibt die fachliche Bedeutung der Ressource.
  dcterms:description "Event end must be later than event start."@en ;
  # Diese Aussage benennt die maschinenentscheidbare Systemeigenschaft per IRI.
  sf:control <https://specforge.dev/control/event_time_interval> ;
  # Diese Aussage legt den Vergleichsoperator der Erwartung per IRI fest.
  sf:operator <https://specforge.dev/operator/equals> ;
  # Diese Aussage legt den einzelnen erwarteten Control-Wert fest.
  sf:expectedValue "end_after_start" ;
  # Diese Aussage verbindet das Requirement mit einer ausführbaren Verification.
  sf:verifiedBy verification:TEST-DATA-001 ;
  # Diese Aussage verweist auf die konkrete Herkunft der Ressource.
  prov:wasDerivedFrom source:calendar-invariants-event-time .

# Diese Verification sendet ein ungültiges Zeitintervall an die Domain-Grenze.
verification:TEST-DATA-001 a sf:Verification ;
  # Diese Aussage legt die stabile Kennung der Ressource fest.
  dcterms:identifier "TEST-DATA-001" ;
  # Diese Aussage benennt den ausführbaren Prüfadapter per IRI.
  sf:verificationAdapter <https://specforge.dev/verification-adapter/domain_invariant> ;
  # Diese Aussage beschreibt die notwendige Vorbereitung der Verification.
  sf:setup "invalid_interval" ;
  # Diese Aussage legt fest, ob die Verification verpflichtend ist.
  sf:mandatory true ;
  # Diese Aussage verbindet die Verification mit ihrer prüfbaren Assertion.
  sf:assertion assertion:TEST-DATA-001 .

# Diese Assertion erwartet HTTP 422 und die Invariante end größer start.
assertion:TEST-DATA-001 a sf:AssertionSpec ;
  # Diese weitere Typangabe klassifiziert dieselbe Ressource zusätzlich.
  sf:responseStatus 422 ;
  # Diese weitere Typangabe klassifiziert dieselbe Ressource zusätzlich.
  sf:invariant "end > start" .

# PRODUCT-001 fordert die Möglichkeit, ein Event anzulegen.
requirement:PRODUCT-001-1.0.0 a sf:RequirementDefinition ;
  # Diese Aussage legt die stabile Kennung der Ressource fest.
  dcterms:identifier "PRODUCT-001" ;
  # Diese Aussage pinnt die konkrete Version der Ressource.
  dcterms:hasVersion "1.0.0" ;
  # Diese Aussage beschreibt die fachliche Bedeutung der Ressource.
  dcterms:description "A user can create an event."@en ;
  # Diese Aussage benennt die maschinenentscheidbare Systemeigenschaft per IRI.
  sf:control <https://specforge.dev/control/event_create> ;
  # Diese Aussage legt den Vergleichsoperator der Erwartung per IRI fest.
  sf:operator <https://specforge.dev/operator/equals> ;
  # Diese Aussage legt den einzelnen erwarteten Control-Wert fest.
  sf:expectedValue "enabled" ;
  # Diese Aussage verbindet das Requirement mit einer ausführbaren Verification.
  sf:verifiedBy verification:TEST-PRODUCT-001 ;
  # Diese Aussage verweist auf die konkrete Herkunft der Ressource.
  prov:wasDerivedFrom source:calendar-product-create .

# Diese Verification beobachtet das Anlegen eines eigenen Events.
verification:TEST-PRODUCT-001 a sf:Verification ;
  # Diese Aussage legt die stabile Kennung der Ressource fest.
  dcterms:identifier "TEST-PRODUCT-001" ;
  # Diese Aussage benennt den ausführbaren Prüfadapter per IRI.
  sf:verificationAdapter <https://specforge.dev/verification-adapter/http_request> ;
  # Diese Aussage beschreibt die notwendige Vorbereitung der Verification.
  sf:setup "owner_create" ;
  # Diese Aussage legt fest, ob die Verification verpflichtend ist.
  sf:mandatory true ;
  # Diese Aussage verbindet die Verification mit ihrer prüfbaren Assertion.
  sf:assertion assertion:TEST-PRODUCT-001 .

# Diese Assertion erwartet HTTP 201 und gespeicherte Eingabedaten.
assertion:TEST-PRODUCT-001 a sf:AssertionSpec ;
  # Diese weitere Typangabe klassifiziert dieselbe Ressource zusätzlich.
  sf:responseStatus 201 ;
  # Diese weitere Typangabe klassifiziert dieselbe Ressource zusätzlich.
  sf:storedMatches true .

# PRODUCT-002 fordert die Möglichkeit, ein eigenes Event zu lesen.
requirement:PRODUCT-002-1.0.0 a sf:RequirementDefinition ;
  # Diese Aussage legt die stabile Kennung der Ressource fest.
  dcterms:identifier "PRODUCT-002" ;
  # Diese Aussage pinnt die konkrete Version der Ressource.
  dcterms:hasVersion "1.0.0" ;
  # Diese Aussage beschreibt die fachliche Bedeutung der Ressource.
  dcterms:description "A user can read an owned event."@en ;
  # Diese Aussage benennt die maschinenentscheidbare Systemeigenschaft per IRI.
  sf:control <https://specforge.dev/control/event_read> ;
  # Diese Aussage legt den Vergleichsoperator der Erwartung per IRI fest.
  sf:operator <https://specforge.dev/operator/equals> ;
  # Diese Aussage legt den einzelnen erwarteten Control-Wert fest.
  sf:expectedValue "enabled" ;
  # Diese Aussage verbindet das Requirement mit einer ausführbaren Verification.
  sf:verifiedBy verification:TEST-PRODUCT-002 ;
  # Diese Aussage verweist auf die konkrete Herkunft der Ressource.
  prov:wasDerivedFrom source:calendar-product-read .

# Diese Verification beobachtet das Lesen eines eigenen Events.
verification:TEST-PRODUCT-002 a sf:Verification ;
  # Diese Aussage legt die stabile Kennung der Ressource fest.
  dcterms:identifier "TEST-PRODUCT-002" ;
  # Diese Aussage benennt den ausführbaren Prüfadapter per IRI.
  sf:verificationAdapter <https://specforge.dev/verification-adapter/http_request> ;
  # Diese Aussage beschreibt die notwendige Vorbereitung der Verification.
  sf:setup "owner_read" ;
  # Diese Aussage legt fest, ob die Verification verpflichtend ist.
  sf:mandatory true ;
  # Diese Aussage verbindet die Verification mit ihrer prüfbaren Assertion.
  sf:assertion assertion:TEST-PRODUCT-002 .

# Diese Assertion erwartet HTTP 200 und die richtige Ressource.
assertion:TEST-PRODUCT-002 a sf:AssertionSpec ;
  # Diese weitere Typangabe klassifiziert dieselbe Ressource zusätzlich.
  sf:responseStatus 200 ;
  # Diese weitere Typangabe klassifiziert dieselbe Ressource zusätzlich.
  sf:resourceMatches true .

# PRODUCT-003 fordert die Möglichkeit, ein eigenes Event zu ändern.
requirement:PRODUCT-003-1.0.0 a sf:RequirementDefinition ;
  # Diese Aussage legt die stabile Kennung der Ressource fest.
  dcterms:identifier "PRODUCT-003" ;
  # Diese Aussage pinnt die konkrete Version der Ressource.
  dcterms:hasVersion "1.0.0" ;
  # Diese Aussage beschreibt die fachliche Bedeutung der Ressource.
  dcterms:description "A user can update an owned event."@en ;
  # Diese Aussage benennt die maschinenentscheidbare Systemeigenschaft per IRI.
  sf:control <https://specforge.dev/control/event_update> ;
  # Diese Aussage legt den Vergleichsoperator der Erwartung per IRI fest.
  sf:operator <https://specforge.dev/operator/equals> ;
  # Diese Aussage legt den einzelnen erwarteten Control-Wert fest.
  sf:expectedValue "enabled" ;
  # Diese Aussage verbindet das Requirement mit einer ausführbaren Verification.
  sf:verifiedBy verification:TEST-PRODUCT-003 ;
  # Diese Aussage verweist auf die konkrete Herkunft der Ressource.
  prov:wasDerivedFrom source:calendar-product-update .

# Diese Verification beobachtet das Ändern eines eigenen Events.
verification:TEST-PRODUCT-003 a sf:Verification ;
  # Diese Aussage legt die stabile Kennung der Ressource fest.
  dcterms:identifier "TEST-PRODUCT-003" ;
  # Diese Aussage benennt den ausführbaren Prüfadapter per IRI.
  sf:verificationAdapter <https://specforge.dev/verification-adapter/http_request> ;
  # Diese Aussage beschreibt die notwendige Vorbereitung der Verification.
  sf:setup "owner_update" ;
  # Diese Aussage legt fest, ob die Verification verpflichtend ist.
  sf:mandatory true ;
  # Diese Aussage verbindet die Verification mit ihrer prüfbaren Assertion.
  sf:assertion assertion:TEST-PRODUCT-003 .

# Diese Assertion erwartet HTTP 200 und gespeicherte Änderungen.
assertion:TEST-PRODUCT-003 a sf:AssertionSpec ;
  # Diese weitere Typangabe klassifiziert dieselbe Ressource zusätzlich.
  sf:responseStatus 200 ;
  # Diese weitere Typangabe klassifiziert dieselbe Ressource zusätzlich.
  sf:storedMatches true .

# PRODUCT-004 fordert die Möglichkeit, ein eigenes Event zu löschen.
requirement:PRODUCT-004-1.0.0 a sf:RequirementDefinition ;
  # Diese Aussage legt die stabile Kennung der Ressource fest.
  dcterms:identifier "PRODUCT-004" ;
  # Diese Aussage pinnt die konkrete Version der Ressource.
  dcterms:hasVersion "1.0.0" ;
  # Diese Aussage beschreibt die fachliche Bedeutung der Ressource.
  dcterms:description "A user can delete an owned event."@en ;
  # Diese Aussage benennt die maschinenentscheidbare Systemeigenschaft per IRI.
  sf:control <https://specforge.dev/control/event_delete> ;
  # Diese Aussage legt den Vergleichsoperator der Erwartung per IRI fest.
  sf:operator <https://specforge.dev/operator/equals> ;
  # Diese Aussage legt den einzelnen erwarteten Control-Wert fest.
  sf:expectedValue "enabled" ;
  # Diese Aussage verbindet das Requirement mit einer ausführbaren Verification.
  sf:verifiedBy verification:TEST-PRODUCT-004 ;
  # Diese Aussage verweist auf die konkrete Herkunft der Ressource.
  prov:wasDerivedFrom source:calendar-product-delete .

# Diese Verification beobachtet das Löschen eines eigenen Events.
verification:TEST-PRODUCT-004 a sf:Verification ;
  # Diese Aussage legt die stabile Kennung der Ressource fest.
  dcterms:identifier "TEST-PRODUCT-004" ;
  # Diese Aussage benennt den ausführbaren Prüfadapter per IRI.
  sf:verificationAdapter <https://specforge.dev/verification-adapter/http_request> ;
  # Diese Aussage beschreibt die notwendige Vorbereitung der Verification.
  sf:setup "owner_delete" ;
  # Diese Aussage legt fest, ob die Verification verpflichtend ist.
  sf:mandatory true ;
  # Diese Aussage verbindet die Verification mit ihrer prüfbaren Assertion.
  sf:assertion assertion:TEST-PRODUCT-004 .

# Diese Assertion erwartet HTTP 204 und anschließend HTTP 404.
assertion:TEST-PRODUCT-004 a sf:AssertionSpec ;
  # Diese weitere Typangabe klassifiziert dieselbe Ressource zusätzlich.
  sf:responseStatus 204 ;
  # Diese weitere Typangabe klassifiziert dieselbe Ressource zusätzlich.
  sf:afterStatus 404 .

# Diese Quelle verweist auf die fachliche Zeitintervall-Policy.
source:calendar-invariants-event-time a prov:Entity ;
  # Diese Aussage klassifiziert die Art der Herkunftsquelle.
  sf:sourceType "architecture_policy" ;
  # Diese Aussage legt die stabile Kennung der Ressource fest.
  dcterms:identifier "calendar-invariants" ;
  # Diese Aussage pinnt die konkrete Version der Ressource.
  dcterms:hasVersion "1.0.0" ;
  # Diese Aussage benennt den relevanten Abschnitt der Herkunftsquelle.
  sf:sourceSection "event-time" .

# Diese Quelle verweist auf die Create-Sektion des Calendar-Produkts.
source:calendar-product-create a prov:Entity ;
  # Diese Aussage klassifiziert die Art der Herkunftsquelle.
  sf:sourceType "product_spec" ;
  # Diese Aussage legt die stabile Kennung der Ressource fest.
  dcterms:identifier "calendar-product" ;
  # Diese Aussage pinnt die konkrete Version der Ressource.
  dcterms:hasVersion "1.0.0" ;
  # Diese Aussage benennt den relevanten Abschnitt der Herkunftsquelle.
  sf:sourceSection "create" .

# Diese Quelle verweist auf die Read-Sektion des Calendar-Produkts.
source:calendar-product-read a prov:Entity ;
  # Diese Aussage klassifiziert die Art der Herkunftsquelle.
  sf:sourceType "product_spec" ;
  # Diese Aussage legt die stabile Kennung der Ressource fest.
  dcterms:identifier "calendar-product" ;
  # Diese Aussage pinnt die konkrete Version der Ressource.
  dcterms:hasVersion "1.0.0" ;
  # Diese Aussage benennt den relevanten Abschnitt der Herkunftsquelle.
  sf:sourceSection "read" .

# Diese Quelle verweist auf die Update-Sektion des Calendar-Produkts.
source:calendar-product-update a prov:Entity ;
  # Diese Aussage klassifiziert die Art der Herkunftsquelle.
  sf:sourceType "product_spec" ;
  # Diese Aussage legt die stabile Kennung der Ressource fest.
  dcterms:identifier "calendar-product" ;
  # Diese Aussage pinnt die konkrete Version der Ressource.
  dcterms:hasVersion "1.0.0" ;
  # Diese Aussage benennt den relevanten Abschnitt der Herkunftsquelle.
  sf:sourceSection "update" .

# Diese Quelle verweist auf die Delete-Sektion des Calendar-Produkts.
source:calendar-product-delete a prov:Entity ;
  # Diese Aussage klassifiziert die Art der Herkunftsquelle.
  sf:sourceType "product_spec" ;
  # Diese Aussage legt die stabile Kennung der Ressource fest.
  dcterms:identifier "calendar-product" ;
  # Diese Aussage pinnt die konkrete Version der Ressource.
  dcterms:hasVersion "1.0.0" ;
  # Diese Aussage benennt den relevanten Abschnitt der Herkunftsquelle.
  sf:sourceSection "delete" .

id: fastapi/read-rate-limit
version: "2.0.0"
owner: platform-team
satisfies: [PLATFORM-001]
stack: fastapi-react
controls: {rate_limit: 60}
verifications: [TEST-PLATFORM-001]
constraints:
  required: [Authenticated reads are rate limited server-side.]
  forbidden: [Client-provided rate counters are trusted.]
recommendations: [Partition counters by authenticated actor.]
artifacts: [backend/app.py]
